// Sample TypeScript source for dependency extraction tests.
// Contains a bare npm import (excluded) and a relative import.

import React from 'react';
import type { Config } from './local_module';

export function sampleFunction(): void {
    console.log('sample');
}
