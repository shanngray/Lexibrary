# init

**Summary:** Package for project initialisation helpers; currently only contains `scaffolder`.

## Dependents

- `lexibrarian.cli` — imports `create_lexibrary_skeleton` from `lexibrarian.init.scaffolder`
