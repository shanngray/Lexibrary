# crawler

**Summary:** Re-exports `full_crawl` and `CrawlStats` from `crawler.engine` as the package's public API.

## Re-exports

`CrawlStats`, `full_crawl`

## Dependents

- `lexibrarian.daemon.service` — imports `full_crawl`
