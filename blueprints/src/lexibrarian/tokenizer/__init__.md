# tokenizer

**Summary:** Re-exports `TokenCounter` protocol and `create_tokenizer` factory as the package's public API.

## Re-exports

`TokenCounter`, `create_tokenizer`

## Dependents

- `lexibrarian.daemon.service` — imports `create_tokenizer`
- `lexibrarian.crawler.engine` — types against `TokenCounter`
