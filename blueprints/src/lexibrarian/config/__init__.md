# config

**Summary:** Re-exports the full config public API: schema models, loader, and default template.

## Re-exports

`DEFAULT_PROJECT_CONFIG_TEMPLATE`, `load_config`, `DaemonConfig`, `IgnoreConfig`, `LexibraryConfig`, `LLMConfig`, `MappingConfig`, `TokenBudgetConfig`

## Dependents

- `lexibrarian.daemon.service` — imports `load_config`, `find_config_file`, `LexibraryConfig`
