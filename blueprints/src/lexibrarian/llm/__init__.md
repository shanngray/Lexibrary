# llm

**Summary:** Re-exports the full LLM public API: service, rate limiter, request/result dataclasses, and factory.

## Re-exports

`DirectorySummaryRequest`, `FileSummaryRequest`, `FileSummaryResult`, `LLMService`, `RateLimiter`, `create_llm_service`

## Dependents

- `lexibrarian.daemon.service` — imports `create_llm_service`
