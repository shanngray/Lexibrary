# daemon

**Summary:** Re-exports `DaemonService` from `daemon.service` as the package's public API.

## Re-exports

`DaemonService`

## Dependents

- `lexibrarian.cli` — will import `DaemonService` when `daemon` command is implemented
