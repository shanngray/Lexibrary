# indexer

**Summary:** Structural `.aindex` generation pipeline — produces and writes directory index files without LLM calls.

## Re-exports

Nothing re-exported; package is empty. Import directly from submodules.

## Dependents

- `lexibrarian.cli` — imports `index_directory`, `index_recursive` from `indexer.orchestrator`
