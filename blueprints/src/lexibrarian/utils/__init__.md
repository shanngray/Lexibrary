# utils

**Summary:** Re-exports the four core utility functions as the package's public API.

## Re-exports

`detect_language`, `hash_file`, `setup_logging`, `find_project_root`

## Dependents

- Various modules import directly from submodules rather than the package `__init__`
